import 'mocha'
import { expect } from 'chai';

describe('DeviceEngine', function() {

    it('should create a device', () => {
        expect({}).not.to.be.undefined;
    });

    it('should create a device with failure', () => {
        expect({}).not.to.be.undefined;
    });

});
